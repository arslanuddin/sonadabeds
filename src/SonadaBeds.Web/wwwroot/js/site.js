document.addEventListener('DOMContentLoaded',()=>{document.querySelectorAll('[data-config-price]').forEach(el=>{el.addEventListener('change',()=>{const base=Number(el.dataset.base||0);let total=base;document.querySelectorAll('[data-option-price]:checked').forEach(x=>total+=Number(x.dataset.optionPrice||0));const target=document.querySelector('[data-total]');if(target)target.textContent='£'+total.toLocaleString('en-GB',{minimumFractionDigits:2});});});});
